# RootGatewayDispatcher

Dispatcher for Root-authorized gateway calls into `DotnsPopController`.

The dispatcher verifies the privileged gateway path before forwarding reservation and claim operations to the PoP controller.
