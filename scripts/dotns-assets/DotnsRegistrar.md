# DotnsRegistrar

ERC721-backed registrar for dotNS names.

The registrar owns name token state and authorizes protocol controllers. Registration policy, pricing, and gateway-specific checks live in the controllers and `PopRules`.
