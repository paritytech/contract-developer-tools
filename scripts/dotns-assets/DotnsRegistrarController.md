# DotnsRegistrarController

Public commit-reveal registration controller.

It validates commitments, applies `PopRules` pricing and eligibility, mints through `DotnsRegistrar`, writes registry records, sets reverse records, coordinates stores, and accounts for refunds or escrowed transfer friction.
