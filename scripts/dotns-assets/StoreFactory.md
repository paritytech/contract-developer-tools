# StoreFactory

Factory for dotNS immutable stores.

It deploys per-label and per-user stores behind beacons, allowing dotNS registration flows to create durable store instances for name and user metadata.
