# DotnsRegistry

Forward registry for dotNS node records.

It tracks owners, resolvers, and subname hierarchy. Base-name ownership is backed by `DotnsRegistrar`, while subnodes carry explicit records.
